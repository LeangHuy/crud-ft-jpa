server:
  port: 8081

spring:
  application:
    name: test-jpa
  datasource:
    url: jdbc:postgresql://localhost:5432/test_jpa
    username: postgres
    password: Huy240419
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
